const fs = require("fs");
const path = require("path");

module.exports = (client) => {

  // All commands are now managed exclusively by the core/commandLoader inside their respective plugins namespace.

};