/**
 * fs.js — DEPRECATED
 *
 * The /fs submit command has been removed.
 * Foster synergy submissions are now handled automatically
 * via the submission thread. Users post directly in the thread:
 *
 *   "initial 450"  +  [screenshot attached]
 *
 * All logic lives in events/messageCreate.js
 */

// Intentionally exports no 'name' or 'data' — will be ignored by commandLoader.
module.exports = {};
